- if current_user
  %a{:id => "groupInvite", :class => "groupActionButton", :href => "/groups/#{@group.permalink}/invite"}
    Invite Someone To Join This Group
- else
  %a{:id => "groupRequest", :class => "groupActionButton", :href => "/groups/#{@group.permalink}/join"}
    Request To Join This Group