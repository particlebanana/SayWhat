%div
  %h2
    Set you group's URL:
    
  %p
    Choose a URL for your group's homepage. It must be unique and can't be changed.
    
  = form_for @group, :url => set_permalink_group_path(@group), :method => :put do |f|
    
    = errors_for(@group, "Opps, there were some problems setting your group's URL").html_safe
    
    %p.group
      = f.label :url
      = f.text_field :permalink
    
    %button.submit{:type => 'submit'}
      Continue