%div
  %h2
    Pending Groups
    
  - if @groups.size > 0
    %ul
      - @groups.each do |group|
        %li
          = link_to group.display_name, "/groups/#{group.id}/pending_group"
          
  - else
    %h3
      No Pending Groups