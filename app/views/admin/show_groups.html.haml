.block
  .content
    %h2.title All Groups
    .inner
      - if @groups.all.empty?
        %p No Groups
      - else
        %table.table
          = render :partial => "admin/partials/groups_table"
       
        
= content_for :sidebar, render(:partial => 'admin/partials/sidebar')