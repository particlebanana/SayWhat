.block
  .content
    %h2.title All Registered Users
    .inner
      - if @users.all.empty?
        %p No Registered Users....Ouch
      - else
        %div#usersTable.adminTableView
          = render :partial => "admin/partials/users_table"