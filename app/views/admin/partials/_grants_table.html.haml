%tr
  %th Group Name
  %th Sponsor Name
  %th.last Sponsor Email
- @grants.each do |grant|
  %tr{:class => cycle("odd", "even")}
    %td= link_to grant.group_name, "#"
    %td= grant.adult_name
    %td= grant.adult_email