= title "Projects"

%div#groupTop.group
  %h2.left
    Projects

%ul#projectNav
  %li
    = link_to "everyone's projects", "/projects"
  %li
    = link_to "new project", "/groups/#{@group.permalink}/projects/new"
    
%ul#projectsList    
  - @projects.each do |project|
    %li
      = image_tag(project.profile_photo_url(:small))
      = link_to project.display_name, "/groups/#{@group.permalink}/projects/#{project.name}"