= title "Sign Up"

.row
  %section.span4.form-description
    %h2.heading Sign-Up to Say What!

    %p Salvia fugiat seitan esse, irony mlkshk DIY high life. Seitan nulla american apparel iphone. Minim aesthetic tumblr skateboard, fanny pack nulla sint letterpress sapiente. Mcsweeney's iphone chambray, sint wolf nulla lo-fi aliquip artisan quinoa cliche lomo gentrify keytar.

    - if controller_name != 'sessions'
      %p= link_to "Sign in to Say What!", new_session_path(resource_name), :class => "btn success"

  %section.span11.offset1
    %fieldset
      %legend Say What! Sign-Up Form

      = form_for(resource, :as => resource_name, :url => registration_path(resource_name)) do |f|

        .clearfix
          = f.label :first_name
          .input
            = f.text_field :first_name, :class => "xlarge"

        .clearfix
          = f.label :last_name
          .input
            = f.text_field :last_name, :class => "xlarge"

        .clearfix
          = f.label :email
          .input
            = f.text_field :email, :class => "xlarge"

        .clearfix
          = f.label :password
          .input
            = f.password_field :password, :class => "xlarge"

        .clearfix
          = f.label :password_again
          .input
            = f.password_field :password_confirmation, :class => "xlarge"

        .actions
          = f.submit "Create Account", :class => "btn primary"
          = link_to "Forgot your password?", new_password_path(resource_name), :class => "btn default"